# Minimal Git Repo

Fixture for Grounder integration tests. Git is initialized in test setup — do not commit `.git/` here.

Used to exercise `grounder init`, project-id detection, and `.grounder.json` generation.
