#!/usr/bin/env node
/**
 * Scaffold stub — Step 2 will replace this with the synthetic asciicast generator.
 * Scene scripts live in ../scenes/; artifacts land in ../out/.
 */
console.error(
  "@grounder/demo-casts: cast generator not implemented yet (see plan demo-casts-package).",
);
process.exit(1);
